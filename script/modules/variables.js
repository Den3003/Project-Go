let startTime = NaN;
const durationOpacity = 300;
const scrollbarWidth = window.innerWidth - document
    .documentElement.clientWidth;

export default {
  startTime,
  durationOpacity,
  scrollbarWidth,
};
